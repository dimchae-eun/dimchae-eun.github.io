# 📖 내 포트폴리오 사이트 수정 가이드 (비개발자용)

안녕하세요! 이 사이트는 **HTML**로 만들어져 있어요.
겁먹지 않으셔도 됩니다. **글자만 바꾸면 되고, 코딩 지식이 전혀 없어도** 이 가이드만 따라 하시면 충분히 수정할 수 있어요.

> 💡 핵심 규칙 딱 하나: **`< >` 로 감싸진 부분(태그)은 건드리지 말고, 그 사이의 "글자"만 바꾸세요.**

---

## 1. 파일이 어떤 역할을 하는지 (지도)

| 파일 이름 | 무엇을 담고 있나 | 언제 여나요 |
|---|---|---|
| `index.html` | 첫 화면 (사진, 자기소개, 관심분야, 학력, 경력) | 소개·사진·학력을 고칠 때 |
| `publications.html` | 논문 목록 | 논문을 추가/수정할 때 |
| `patents.html` | 특허 목록 | 특허를 추가할 때 |
| `news.html` | 소식 타임라인 | 새 소식·수상을 추가할 때 |
| `cv.html` | 이력서(CV) 페이지 | 이력을 고칠 때 |
| `assets/img/` | 사진이 들어가는 폴더 | 프로필 사진을 넣을 때 |
| `assets/files/` | CV PDF가 들어가는 폴더 | 이력서 PDF를 올릴 때 |
| `assets/css/style.css` | 디자인(색·글씨·모양) | 디자인을 바꿀 때만 (평소엔 안 건드림) |

파일을 열 때는 **메모장(윈도우)** 이나 **텍스트 편집기(맥)**, 또는 아래 3번에서 설명하는 **GitHub 웹사이트에서 직접** 열면 됩니다.

---

## 2. 자주 하는 수정 — 그대로 따라 하기

### ✏️ (A) 프로필 사진 넣기

1. 넣고 싶은 사진 파일 이름을 `profile.jpg` 로 바꿉니다. (정사각형 사진이 가장 예뻐요)
2. 그 파일을 `assets/img/` 폴더 안에 넣습니다.
3. `index.html` 파일을 열고, 아래 줄을 찾습니다. (`profile__avatar` 로 검색하면 빨리 찾아요)

   ```html
   <img class="profile__avatar" src="assets/img/profile.svg" alt="Chaeeun Kim" />
   ```
4. `profile.svg` 를 `profile.jpg` 로만 바꿉니다.

   ```html
   <img class="profile__avatar" src="assets/img/profile.jpg" alt="Chaeeun Kim" />
   ```
5. 저장 끝!

---

### ✏️ (B) 자기소개 글 고치기

`index.html` 을 열고 `<p>` 로 시작하는 문장을 찾아 **문장만** 바꾸면 됩니다.
예를 들어:

```html
<p class="hero__tagline">A proactive, evolving, and self-driven researcher.</p>
```

여기서 큰따옴표 없이 가운데 영어 문장만 원하는 내용으로 바꾸세요.
`<p ...>` 와 `</p>` 는 그대로 두어야 합니다.

---

### ✏️ (C) 논문 추가하기 (가장 자주 쓰는 기능)

`publications.html` 을 엽니다. 논문 하나는 아래처럼 `<li class="pub">` 로 시작해서 `</li>` 로 끝나는 한 덩어리예요.
**이 덩어리 전체를 복사해서 바로 위(또는 아래)에 붙여 넣고**, 글자만 바꾸면 됩니다.

```html
<li class="pub">
  <div class="pub__year">2026</div>                          <!-- ← 연도 -->
  <div class="pub__body">
    <h2 class="pub__title">여기에 논문 제목</h2>              <!-- ← 제목 -->
    <p class="pub__authors">저자1, <span class="me">Chaeeun Kim</span>, 저자3</p>  <!-- ← 저자. 본인 이름은 <span class="me"> 로 감싸면 밑줄+강조됩니다 -->
    <div class="pub__venue">
      <span class="pub__journal">저널 이름</span>            <!-- ← 저널명 -->
      <span class="badge badge--if">IF 11.8</span>          <!-- ← Impact Factor. 없으면 이 줄을 지우세요 -->
      <a class="pub__link" href="https://doi.org/여기에DOI" target="_blank" rel="noopener">   <!-- ← DOI 링크 -->
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10 13a5 5 0 0 0 7 0l3-3a5 5 0 0 0-7-7l-1 1"/><path d="M14 11a5 5 0 0 0-7 0l-3 3a5 5 0 0 0 7 7l1-1"/></svg>
        DOI
      </a>
    </div>
  </div>
</li>
```

바꿀 곳은 딱 4가지: **연도 / 제목 / 저자 / 저널·IF·DOI**. 나머지(`<svg ...>` 같은 그림 코드)는 절대 건드리지 마세요.

---

### ✏️ (D) 새 소식(News) 추가하기

`news.html` 을 엽니다. **최신 소식이 맨 위**에 오도록 되어 있어요.
소식 하나는 아래 한 덩어리입니다. 맨 위 덩어리를 복사해서 제일 위에 붙여 넣고 글자만 바꾸세요.

```html
<li class="news-item is-highlight">                <!-- 강조하고 싶으면 is-highlight 를 남기고, 아니면 지우세요 -->
  <span class="news-item__date">Mar 2026</span>    <!-- ← 날짜 -->
  <p class="news-item__text">여기에 소식 내용</p>   <!-- ← 내용 -->
</li>
```

- **수상 소식**이면 내용 끝에 `<span class="tag tag--award">Award</span>` 를 붙이면 노란 "Award" 배지가 생겨요.
- 링크를 넣고 싶으면: `<a href="주소" target="_blank" rel="noopener">보이는 글자</a>`

---

### ✏️ (E) 아직 비어 있는 항목 채우기 (지금은 숨겨져 있음)

일부러 **숨겨 놓은** 부분들이 있어요. 내용을 채운 뒤, 그 줄의 `style="display:none"` 만 지우면 화면에 나타납니다.

1. **2023년 7월 소식** — `news.html` 에서 `Jul 2023` 을 찾아 내용을 적고 `style="display:none"` 삭제
2. **Google Scholar / LinkedIn 아이콘** — `index.html` 에서 `Google Scholar` / `LinkedIn` 을 찾아 `href="#"` 의 `#` 을 실제 주소로 바꾸고 `style="display:none"` 삭제
3. **CV PDF 다운로드 버튼** — 아래 (F) 참고

---

### ✏️ (F) 이력서(CV) PDF 올리기

1. 이력서 PDF 파일 이름을 `CV_Chaeeun_Kim.pdf` 로 바꿉니다.
2. `assets/files/` 폴더에 넣습니다.
3. `cv.html` 을 열고 `btn-download` 를 찾은 뒤, 그 줄의 `style="display:none"` 을 지우면 "Download PDF" 버튼이 나타납니다.

---

## 3. 인터넷에 올리기 (GitHub 업로드 & 사이트 켜기)

이 사이트는 **GitHub**라는 곳에 파일을 올리면 자동으로 인터넷 주소로 공개됩니다.
현재 주소: **https://dimchae-eun.github.io/Chaeeun-Kim.github.io/**

### 처음 한 번만 하는 설정

1. 웹브라우저에서 본인 저장소로 갑니다 → `https://github.com/dimchae-eun/Chaeeun-Kim.github.io`
2. **`Add file` → `Upload files`** 버튼을 누릅니다.
3. 이 폴더 안의 **모든 파일과 폴더를 통째로 드래그**해서 올립니다.
   (`.nojekyll` 이라는 점으로 시작하는 파일도 꼭 함께 올려주세요.)
4. 아래 초록색 **`Commit changes`** 버튼을 누릅니다.
5. 저장소 위쪽 **`Settings` → 왼쪽 메뉴 `Pages`** 로 갑니다.
6. **Branch** 를 `main` 으로, 폴더는 `/ (root)` 로 선택하고 **Save**.
7. 1~2분 기다리면 위 주소에서 사이트가 열립니다. 🎉

### 나중에 내용을 고칠 때 (가장 쉬운 방법)

GitHub 웹사이트에서 **직접** 고치는 게 제일 편해요:

1. 저장소에서 고치고 싶은 파일(예: `news.html`)을 클릭합니다.
2. 오른쪽 위 **연필 아이콘(✏️ Edit this file)** 을 누릅니다.
3. 글자를 고칩니다. (이 가이드 2번 참고)
4. 초록색 **`Commit changes`** 를 누르면 1~2분 뒤 사이트에 반영됩니다.

사진이나 PDF 같은 **파일을 추가**할 때만 위의 `Upload files` 방법을 쓰면 됩니다.

---

## 4. 실수했을 때 / 주의사항

- **`< >` 태그를 지우면 화면이 깨질 수 있어요.** 항상 "글자"만 바꾸세요.
- 여는 태그 `<p>` 와 닫는 태그 `</p>` 는 **항상 짝**입니다. 하나만 지우지 마세요.
- 복사-붙여넣기로 항목을 늘릴 때는 `<li ...>` 부터 `</li>` 까지 **한 덩어리 통째로** 복사하세요.
- 겁나면, 고치기 전에 원본 파일을 컴퓨터에 하나 복사해 두세요. 잘못돼도 되돌릴 수 있어요.
- GitHub는 수정 기록이 자동 저장돼서, 최악의 경우 이전 버전으로 되돌릴 수 있습니다.

---

## 5. 다크모드 · 모바일

- 오른쪽 위 **달 아이콘** 을 누르면 밝은/어두운 화면이 바뀝니다. (자동 저장됨)
- 휴대폰에서도 자동으로 보기 좋게 정렬됩니다. 따로 설정할 필요 없어요.

---

궁금한 게 생기면 이 파일을 다시 열어 보세요. 화이팅! 🌱
